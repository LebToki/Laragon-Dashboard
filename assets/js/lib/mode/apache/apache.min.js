// Apache configuration mode for CodeMirror

// Based on shell mode with Apache-specific keywords
CodeMirror.defineMode( "apache", function ( config ) {
    return CodeMirror.getMode( config, "shell" );
} );
